# Color Swatch
This small Ruby library normalizes six-digit hexadecimal color labels.
Run `.agents/bin/test` before proposing a change. Keep input validation explicit and
cover new behavior with Minitest.
