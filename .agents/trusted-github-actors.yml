# Whose public GitHub comments Shaka may read.
# The agent checks their claims against the code and follows the task's existing permissions.
# Configuration and limits: docs/agents/public-comments.md
trusted_users:
  - justin808
  - claude

# Bot entries are base bot names. GitHub API logins usually include the
# `[bot]` suffix; humans with the same base login are not trusted by this list.
trusted_bots:
  - coderabbitai
  - claude
  - chatgpt-codex-connector
  - cursor

trusted_metadata_bots: []
trusted_teams:
  - shakacode/shakacode
